The existence of this file in the users root directory causes macOS to
silence the Message Of The Day (MOTD) and login message when launching a
terminal instance. It will also mute the /etc/motd file from executing.
